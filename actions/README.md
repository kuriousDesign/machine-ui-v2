# Server Actions

Put thin Next server action entrypoints here.

These should validate or orchestrate app-facing mutations, then delegate to
`lib/services/` or machine-specific action wrappers under `machines/`.